import 'package:flutter/material.dart';

class KinerjaMedis extends StatelessWidget {
  const KinerjaMedis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            "Not Found"
        ),
      ),
    );
  }
}
